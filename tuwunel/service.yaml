apiVersion: v1
kind: Service
metadata:
  name: tuwunel
  namespace: matrix
  labels:
    app.kubernetes.io/name: tuwunel
    app.kubernetes.io/component: homeserver
spec:
  selector:
    app.kubernetes.io/name: tuwunel
  ports:
    - name: http
      port: 6167
      targetPort: 6167
      protocol: TCP
  type: ClusterIP
