# Real secrets - never commit
secrets/*.yaml
secrets/*.yaml~
secrets/deployed/
!secrets/examples/

# Ansible vault (encrypted secrets)
ansible/vault-vars.yml

# Separate repo (independently managed)
spp-event-bot/

# OS files
.DS_Store
*.swp
*~
