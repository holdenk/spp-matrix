apiVersion: v1
kind: Secret
metadata:
  name: event-bot-secrets
  namespace: matrix
type: Opaque
stringData:
  # Get this after creating the bot user in Tuwunel admin room:
  #   !admin users create @event-bot:sparklingpinkpandas.com <password>
  # Then log in as the bot to obtain an access token
  MATRIX_ACCESS_TOKEN: "CHANGE_ME"
