apiVersion: v1
kind: Secret
metadata:
  name: pg-backup
  namespace: matrix
type: Opaque
stringData:
  PG_ACCESS_KEY_ID: "{{ pg_backup_access_key_id }}"
  PG_ACCESS_SECRET_KEY: "{{ pg_backup_secret_access_key }}"
