apiVersion: v1
kind: Secret
metadata:
  name: rclone-secrets
  namespace: matrix
type: Opaque
stringData:
  bucket: "{{ b2_bucket }}"
  rclone.conf: |
    [b2]
    type = b2
    account = {{ b2_key_id }}
    key = {{ b2_app_key }}
